# ── Database (local dev — Railway manages production) ──
# Ports offset to avoid MadWorld conflicts: PG=5433, Redis=6381, API=8001
DATABASE_URL=postgresql+asyncpg://rgdgc:rgdgc_dev@localhost:5433/rgdgc
DB_USER=rgdgc
DB_PASS=rgdgc_dev

# ── Redis (local dev) ──
REDIS_URL=redis://localhost:6381

# ── Security ──
SECRET_KEY=generate-a-64-char-random-string
JWT_SECRET=generate-another-64-char-random-string
JWT_EXPIRY=3600

# ── Application ──
ENVIRONMENT=development
CORS_ORIGINS=http://localhost:8081,http://localhost:5173

# ── Mobile (Expo) ──
EXPO_PUBLIC_API_URL=http://localhost:8001

# ── Admin Dashboard ──
VITE_API_URL=http://localhost:8001

# ── File Storage ──
# S3_BUCKET=rgdgc-uploads
# S3_REGION=us-east-1
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# ── Blockchain (P1) ──
# WEB3_PROVIDER_URL=https://eth-sepolia.g.alchemy.com/v2/YOUR_KEY
# RGDG_TOKEN_ADDRESS=0x...
# TREASURY_ADDRESS=0x...

# ── MCP Server ──
RGDGC_API_URL=http://localhost:8000
RGDGC_API_KEY=

# ── Anthropic (for OpenClaw / bot) ──
# ANTHROPIC_API_KEY=

# ── Messaging Platforms (OpenClaw) ──
# TELEGRAM_BOT_TOKEN=
# DISCORD_BOT_TOKEN=

# ── Monitoring ──
# SENTRY_DSN=
# POSTHOG_API_KEY=

# ── Expo (CI/CD) ──
# EXPO_TOKEN=
