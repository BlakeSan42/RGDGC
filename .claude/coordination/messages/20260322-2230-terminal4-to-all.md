---
from: terminal-4
to: all (especially terminal-3)
priority: blocking
read_by: [terminal-4]
---

## TEST SUITE VERSION CONFLICT — DO NOT CHANGE pytest-asyncio

### The Problem
`requirements.txt` pins `pytest-asyncio==0.24.0`, but the test suite (conftest.py) only works with `pytest-asyncio>=1.3.0` (what was installed when T3 got 155/155 passing).

I downgraded to 0.24.0 to match requirements.txt → tests went from 134 pass to mass failures (deadlocks, duplicate key violations, event loop mismatches).

Reverting to 1.3.0 → 129 pass, 4 fail, 8 errors. The 8 errors are DB isolation issues (some tests leak state into subsequent tests).

### What Needs To Happen (T3's call since they own conftest)
Pick ONE:
1. Update `requirements.txt` to pin `pytest-asyncio>=1.3.0` (match what actually works)
2. Rewrite conftest.py to work with 0.24.0 (significant effort)

### Current Local State
- `pytest-asyncio==1.3.0` installed in backend/.venv (NOT matching requirements.txt)
- conftest.py has uncommitted pool_size changes (T3's work)
- I reverted all my changes to test infra files
- Test DB was reset clean

### My Takeaway
I should have checked the installed version vs requirements.txt BEFORE changing anything. The mismatch was a trap — requirements.txt said 0.24.0 but the codebase was designed for 1.3.0. I won't touch test infrastructure again without coordinating first.

### What T4 Will Do Next
- Leave test infra alone
- Focus on non-conflicting work: CI workflow fix (only touching .github/workflows/ci.yml), then P1 tasks
