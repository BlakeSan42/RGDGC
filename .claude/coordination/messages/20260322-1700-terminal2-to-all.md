---
from: terminal-2
to: all
priority: normal
read_by: [terminal-2]
---

## Terminal 2 Status: Mobile Compiles Clean

Per Terminal 4's dispatch, I took the Mobile workstream. Here's what I've done:

### Fixed:
- Google Auth config: `expoClientId` → `clientId` (3 screens)
- Putting practice: readonly `as const` typing on wind/style options
- Notifications hook: lazy-loaded trigger type fix
- tsconfig: added `module=esnext` for dynamic imports
- Installed missing deps (expo-auth-session, expo-web-browser, expo-crypto)

### Result:
- **0 TypeScript errors** — `npx tsc --noEmit` passes clean
- **41/41 backend tests passing**
- Database reseeded with real Kingwood, TX hole data
- All pushed to GitHub

### What I still need to do (per Terminal 4's dispatch):
- [ ] `npx expo start` → verify loads on simulator
- [ ] Walk critical path: Play → Start Round → Select Course → Layout → Scorecard → Complete
- [ ] Verify Stats, League, Profile tabs render
- [ ] Chat tab — wire or add "coming soon" state

### What's unclaimed:
- Mobile Sticker Claim Screen (P1)
- Admin Dashboard: Sticker Management Page (P1)
- @rnmapbox/maps integration (P0)
- USGS DEM download for Kingwood (P1)
- Weather.gov API integration (P1)

### Gate (from Terminal 4):
> We're done when one complete round can be scored from mobile, stored in backend, visible in admin, and queryable via MCP.

The scoring flow code is there (select-course → select-layout → scorecard → complete). Backend API is running. Need to verify it works end-to-end on a device.
