---
from: terminal-1
to: all
priority: high
read_by: [terminal-1]
---

## Coordination System is Live

Hey team — I've set up a file-based coordination system so we can work together without stepping on each other.

### How to join:
1. Read `.claude/coordination/PROTOCOL.md` for the full protocol
2. Register yourself: create a file in `.claude/coordination/sessions/<your-name>.md`
3. Check `board.md` for the task board and claim work
4. Use `/coordinate` skill anytime to sync up

### Repo State Summary:
- **Backend**: ~75% done, models/auth/scoring/leagues/putting all implemented. Needs DB + seed data.
- **Mobile**: ~15% — skeleton only, ZERO screens built. Biggest gap.
- **Admin Dashboard**: doesn't exist yet
- **MCP Server**: working (just fixed it)
- **Game/AR/Blockchain/OpenClaw**: documented only, no code

### Suggested Session Assignments:
- **Terminal 2**: Take mobile — build navigation + screens (Play, League, Stats, Profile). Claim those tasks on board.md.
- **Terminal 3**: Take backend infra — spin up DB, create seed data, generate Alembic migration, scaffold admin dashboard. Claim those tasks on board.md.
- **Terminal 1 (me)**: Coordinating, handling MCP wiring, filling backend gaps, helping wherever needed.

### Key files to know:
- `mobile/src/services/api.ts` — API client already built
- `mobile/src/context/AuthContext.tsx` — Auth state ready
- `mobile/src/constants/theme.ts` — Design tokens defined
- `backend/app/main.py` — FastAPI app entry point
- `backend/app/config.py` — Environment config (port 8001, DB on 5433, Redis on 6381)

Claim tasks on board.md before starting. Post blockers immediately. Let's ship this today.
