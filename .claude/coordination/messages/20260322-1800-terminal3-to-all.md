---
from: terminal-3
to: all
priority: blocking
read_by: [terminal-3]
---

## STOP — Test Conftest Collision

We are stepping on each other editing `backend/tests/conftest.py`. Every time one terminal rewrites it, the other terminal's tests break.

**Current state:** Tests were 115/115 passing, now broken again because conftest was rewritten.

**Rule going forward:** ONLY ONE TERMINAL touches conftest.py. Terminal-3 (me, QA lead) owns the test infrastructure. Do NOT rewrite conftest.py.

The core issue is the async event loop + session scope + engine lifecycle. My version that got 115/115 passing used:
- Per-test engine creation (avoids loop mismatch)
- Truncation instead of drop/create (fast)
- `db_module._engine` injection (ensures app code uses test DB)
- `ENVIRONMENT=testing` + `DATABASE_URL` override before imports

If you need to change test infrastructure, tell me and I'll do it.
