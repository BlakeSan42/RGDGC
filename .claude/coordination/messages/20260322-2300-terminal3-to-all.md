---
from: terminal-3
to: all
priority: normal
read_by: [terminal-3]
---

## Terminal 3 — Bot Deployment Prep Complete

### Claiming: "OpenClaw bot → Go live" (P0 ship blocker)

### What I did this session (resumed after freeze):
1. **Audited full ace-bot codebase** — Telegram bot, API client, AI handler w/ Claude tool-use, formatters, scheduler. All solid.
2. **Removed dead Discord references** — scheduler.py (discord callback), api_client.py (discord ID resolver), __init__.py, formatters.py docstrings
3. **Created `ace-bot/railway.json`** — Dockerfile builder config for Railway deploy
4. **Created `ace-bot/.dockerignore`** — Prevents .venv, .env, __pycache__, .git from bloating the image
5. **Verified all imports pass** — `python -c "from bot.telegram_bot import TelegramBot; ..."` → OK

### Bot status: READY FOR TOKENS
Another terminal already did great work upgrading the bot:
- CLI mode (interactive terminal chat) — works without any tokens
- Telegram mode — needs `TELEGRAM_BOT_TOKEN`
- AI features — optional, needs `ANTHROPIC_API_KEY`
- Weather command added to both CLI and Telegram
- Graceful degradation when AI handler is None

### Blockers:
- **TELEGRAM_BOT_TOKEN** — Blake needs to create via @BotFather
- **RGDGC_API_URL** — needs Railway backend URL (or localhost for testing)
- **ANTHROPIC_API_KEY** — optional, for /ask and AI chat

### Files touched (ace-bot/ only):
- `bot/scheduler.py` — removed Discord callback
- `bot/api_client.py` — removed Discord ID resolver
- `bot/__init__.py` — updated docstring
- `bot/formatters.py` — updated docstring
- `railway.json` — NEW
- `.dockerignore` — NEW

### No conflicts: I only touched ace-bot/ files that no other terminal is working on.
