---
from: terminal-3 (team lead)
to: all
priority: high
read_by: [terminal-3]
---

## Sticker Backend is LIVE — Unblocked for Frontend Work

All 6 sticker endpoints are deployed and tested end-to-end:

```
POST /api/v1/stickers/generate-batch    (admin) — generate codes for printing
GET  /api/v1/stickers/batch/{id}/csv    (admin) — download Avery mail merge CSV
GET  /api/v1/stickers/batch/{id}/inventory (admin) — batch status
GET  /api/v1/stickers/stats             (admin) — overall sticker stats
POST /api/v1/stickers/claim/{code}      (auth)  — claim a sticker to your account
GET  /api/v1/stickers/validate/{code}   (public) — check if code is valid/available
```

### What was done:
- Created `backend/app/models/sticker.py` (StickerOrder + StickerInventory)
- Created `backend/app/api/v1/stickers.py` (full router, 6 endpoints)
- Registered in `router.py` and `models/__init__.py`
- Generated + applied Alembic migration `34ac9bf1c5f8_add_sticker_tables`
- Fixed timezone issue in claim endpoint
- Moved `generate_disc_codes.py` to `scripts/`
- Integration tested: generate 5 codes → validate → claim with disc details → verify stats update
- All 41 existing tests still pass

### Now unblocked — Terminal-1 and Terminal-2 can work on:
1. **Mobile Sticker Claim Screen** — QR scanner + claim flow + disc details form
2. **Admin Dashboard: Sticker Management** — generate batch UI, inventory view, CSV download
3. **Mobile Putting Practice** — still unclaimed

### Phase B (Geo/Mapping) is next after frontend sticker work lands.
